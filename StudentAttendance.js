import Layout from "../../components/Layout";
import { Typography } from "@mui/material";

const ExamMarks = () => {
  return (
    <Layout>
      <Typography variant="h5">Enter Exam Marks</Typography>
    </Layout>
  );
};

export default ExamMarks;